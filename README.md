<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta http-equiv="X-UA-Compatible " content="IE=edge" />
  <meta name="viewport"
    content="width=device-width, initial-scale=1.0, minimum-scale=1.0, maximum-scale=1.0, user-scalable=no" />
  <title>在线咨询</title>
  <style>
    html,
    body {
      width: 100%;
      height: 100%;
      margin: 0;
      padding: 0
    }
  </风格>
</head>
<身体>
  <script type="text/javascript">
    功能 解析(query) {
      变容体 qs = {};
      变容体 我 = query.indexOf('?');
      如果 (i < 0 && query.indexOf('=') < 0) {
        return qs;
      } 其他的 如果 (i >= 0) {
        query = query.substring(i + 1);
      }
      变容体 parts = query.split('&');
      for (变容体 n = 0; n < parts.length; n++) {
        变容体 part = parts[n];
        变容体 key = part.split('=')[0];
        变容体 val = part.split('=')[1];
        key = key.toLowerCase();
        如果 (typeof qs[key] === 'undefined') {
          qs[key] = decodeURIComponent(val);
        } 其他的 如果 (typeof qs[key] === 'string') {
          变容体 arr = [qs[key], decodeURIComponent(val)];
          qs[key] = arr;
        } 其他的 {
          qs[key].推(decodeURIComponent(val));
        }
      }
返回qs；
    }
    功能 初始化() {
      (功能 (m, 没有, q, 我, 一种, j, 的) {
m[我] =
m[我] ||
          功能 () {
            (m[我].一种 = m[我].一种 || []).推(参数);
          };
        (j=不。创建元素(q)), (s=否。getElementsByTagName(q)[0]);
杰异步 = 真实的;
杰字符集 = 'UTF-8';
杰src='https://static.meiqia.com/widget/loader.js';
S父节点.插入前(j, s);
      })(窗口窗口、文档, “脚本”, 梅希亚);
      变容体 data = 解析(窗户位置.搜索);
      变容体=数据。数据显示;
      如果 (对象的。原型。到字符串。呼叫(结束标识) === 【物件阵列】) {
恩德号=+恩德号[0];
      } 其他的 {
Ent Id=+entId
      }
      _美琪亚(已进入, 'bc872db55575a28546e09b791a6e92f7'。。。输入);
      _美琪亚(独立的, 功能 (配置) {
如果（配置color）{如果 (配置color) {
文件中。身体.风格[背景色] = '#'+config。颜色;
        }
        如果 (配置网址) {如果 (配置网址) {
文件中。身体.风格[背景图像] = 'url（'+配置网址+')';
文件中。身体.风格[背景重复] = 不重复;
文件中。身体.风格[背景大小] = '100% 100%';
        }
      });
      _美琪亚(没有Btn);_美琪亚(没有Btn);
      如果 (数据元数据) {如果 (数据元数据) {
        尝试 {
          变容体元数据=JSON.解析(数据元数据);
          _美琪亚（元数据，元数据）；_美琪亚('metadata'，元数据);
}捕捉（e）{}} 捕捉 (e) { }
      }
      如果 (数据语言) {如果 (数据语言) {
        如果 (数据语言本地化！==真的) {如果 (数据语言本地化！==真的) {
          _美琪亚(“语言”，数据的时候。语言);_美琪亚(“语言”，数据的时候。语言);
        }
      }
      如果 ( === 真的) {如果 ( === 真的) {
<!文档类型>_美琪亚('语言地区', 真实的);
<html lang=“en”>}
<头if(数据子源) {头if(数据子源) {
<元字符集=“UT F—8”/>_美琪亚（“子源”，数据的时候.子源）；8” />_美琪亚("子源"，数据的时候。子源);
<meta http-equiv=“X-UA-Compatible” content=“IE=边缘“ />}}
<元名==“视口”如果 (数据可依靠的东西) {“视口”如果 (数据可依靠的东西) {
内容=“宽度=设备宽度，初始比例尺=1.0，最小比例尺=1.0，最大比例尺=1.0，用户可伸缩性=NO”/>_美琪亚(“倒退”，+的数据。可依靠的东西);_美琪亚(“倒退”，+的数据。可依靠的东西);
  <title>在线咨询</标题
<style>如果（数据克里恩蒂德）{如果(数据克里恩蒂德) {
-html，_MEIQIA(客户端标识，数据的时候。克里恩蒂德)-html，_MEIQIA(客户端标识，数据的时候。克里恩蒂德)-html，_MEIQIA(客户端标识，数据的时候。克里恩蒂德);html，_MEIQIA(客户端标识，数据的时候。克里恩蒂德);
主体}}}}
宽度：100%;如果 (数据代理数据群) {{(数据代理数据群) {{
身高：100%_美琪亚(转让人, {代理令牌数据.代理||空，组令牌数据.组||空});(转让人, {代理令牌数据.代理||空，组令牌数据.组||空});(转让人, {代理令牌数据.代理||空，组令牌数据.组||空});(转让人, {代理令牌数据.代理||空，组令牌数据.组||空});
保证金：0；}}}}}}}}
, {{
问候语：数据。问候 || 
<代理标记：数据.代理|空
</头组令牌：数据组||空
《身体》});});});});
<脚本类型=文本/Javascript>
函数解析（查询）{初始化();{初始化();{初始化();{初始化();
变量 qs = {};</脚本>{};</脚本>{};</脚本>{};</脚本>
变量i=查询。索引的</身体>index Of</身体>
如果（i<0和查询。指数之的）<0</html >
